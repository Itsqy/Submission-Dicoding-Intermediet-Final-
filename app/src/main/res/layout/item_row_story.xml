<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp">


    <ImageView
        android:id="@+id/img_story"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_launcher_background"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_title_story"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/bg_btn_primry"
        android:fontFamily="@font/poppins_medium"
        android:paddingHorizontal="12dp"
        android:text="@string/kebakaran_pada_hutan"
        android:textSize="22sp"
        app:layout_constraintBottom_toTopOf="@id/tv_desc_story" />

    <TextView
        android:id="@+id/tv_desc_story"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="22dp"
        android:background="@color/bg_btn_sec"
        android:ellipsize="end"
        android:maxLength="50"
        android:paddingHorizontal="12dp"
        android:text="@string/description_story"
        app:layout_constraintBottom_toBottomOf="@id/tv_title_story" />


</androidx.constraintlayout.widget.ConstraintLayout>